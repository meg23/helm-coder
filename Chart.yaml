apiVersion: v2
name: coder-full
description: Deploy Coder + PostgreSQL in one chart with dynamic credentials
type: application
version: 0.1.0
appVersion: "2.22.1"

dependencies:
  - name: postgresql
    version: "15.5.3"
    repository: https://charts.bitnami.com/bitnami
